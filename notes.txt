Frontend --> HTML
	----------------------------
	The html can be used to make the structure the of the page. 
	The full form of html is hypertext markup language. 
	The html is markup type language.  
	
	What is mean by hypertext?
	----------------------------------
	hyperText mean a text inside the another text.
	
	What is mean by markup language?
	--------------------------------------------
	The markup language uses the angular tag based (brackets.)
	
	Example:
	<h1>  <h1 />
	
	There are total two types markup language.
	
	1. html
	2. xml (Extensible markup language) 
		--> To make the confguration
		--> To transfer the data over the internet / server
		--> The drawback is it slow in nature. 
		--> To solve this problem we use json(javascript object notation)
		
	Q. Who developed the html?
	------------------------------------------------------
	The html developed by Tim Bearner Lee in 1991. In market the first html version came in 1995.
	Tim Bearner Lee also develop www, http, url, html
	
	SEO --> Search Engine Optimization  --> keywords 
	
	google closes the SEO in 2009 ---> security reasons
	
	bytecode --> IoT 
	
	google ---> best IoT based company 
	
	www.bytecode.com  --> no traffic
	
	.webp  --> google
	16mb  
	300kb
	
	.jpg, .jpeg, .png,.gif, .bit
	
	HTML5  ---> html + css + js
	HTML4/3  --> jquery + js

=============================================================================================
Basic Tags
---------------------
1. p --> paragraph --> It will display static information about the product , person or any object.  

2. img ---> if i want to take images on web pages than i can use the tag called <img />
			The img tag require some attributes that are listed below,
				a) src --> source --> image location
				b) alt --> alternative text 
				c) width --> 
				d) height 
				e) map 

3. a --> anchor tag --> To navigate between the pages. I want to navigate(switch) home page to about page. 

Syntax:
----------------------
<a href="path" target="_self, _blank">About</a>

href ---> hypertext reference


4. pre: pre formatted text. We want to use as it is structure.

html entites  --> https://www.toptal.com/designers/htmlarrows/symbols/

5. marquee: If i want to move text or images or any object from left to right, right to left, top to bottom or bottom to top than i can use the marquee tag 

The marquee has some attributes that are listed below,
	1) direction --> left, right, up, down 
	2) scrollamount --> It controls the speed. 
	3) javascript event --> onmouseover --> to stop current object 
	4) javascript event --> onmouseleave --> to start current object

6. mark: This tag can be used in to highlight text.

Note: Every html element has style.

7. span: span is working as container.
8. <b></b> and <strong></strong> --> to make the text bold
9. <i></i> and <em></em>  --> to make text itelic
10. <u></u> to draw the underline
11. <hr> horizantal line
------------------------------------------------------------------------------------------

UL and OL tags:
--------------------------------
Q. What is the basic use of ul and ol tags?
----------------------------------------------------------
The ol and ul tags can be used in to create a dropdown menu. 

1. UL -->(Unordered List):
---------------------------------------
- This is an Unordered list, by default it will display in bullets(disk).
- This tag can be used in to display the information pointwise or we can also create the categroywise list. 
- Suppose, we want to change disk into circle, square we need to pass one attribute called
  as type attribute. The type attribute is depricated for ul tag. So my question is how i can change the type ?
- To solve the above problem we use css property.
		a) list-style-type 
		b) list-style
		c) list-style-image --> to take the image

Q. how we can create nested ul? 
------------------------------------------
please check the Example

OL -->(Ordered List):
-------------------------------
- This is an ordered list, by default it will display in numbers like 1,2,3.
- This tag can be used in to display the information pointwise or we can also create the categroywise list. 
- Suppose, we want to change numbers into alphabets, romans number we need to pass one attribute called as type attribute. The type attribute is not depricated for ol tag. 
- Can we change type by using css in ol tag?
	Yes, you can use the following css properties,
		a) list-style-type 
		b) list-style
		c) list-style-image --> to take the image


=======================================================================================
Table Tag
--------------------------
database 
	tables --> we can store the data in form of rows and columns. 

So, the table tag can be used for to display the data in structured format to the users.
Second advantge of table tag is to make the proper alignment.
Thrid advantage of table tag is, it can be used in to make the website layout. We use it before 10yrs ago. 

The following are some tags used to make tables. 
----------------------------------------------------
1. table --> to make the table
2. th --> table heading(columns) --> font bold + text center
3. tr --> table row
4. td --> table data 
5. thead --> It is group of heading 
6. tbody --> It is group of table rows

I want to make the table of student, so the column names should be like this,
1. SName 
2. SAge 
3. SMob 
4. SCity 
5. STotalMarks

rowspan and colspan 
-----------------------------------
can be used in to join rows or columns
=========================================================================================
Image Mapping
------------------------------
To create the clickable area on image is called as image Mapping.

To perform image Mapping the html introduced two tags 
	a) map 
	b) area 
In each map we create multiple areas.
=====================================================================================
audio and video
----------------------
<audio controls loop muted>
	<source src="audio path" />
</audio>

<video controls autoplay loop muted>
	<source src="" />
</video>
====================================================================================

Form tag 
------------------------------------
What is the use of form?
---------------------------------
Form tag can be used in to collect the user data. 
In html the form tag has some attributes which are listed below, 
1. method: 
	1. get  ---> to get the data from table 
	2. post ---> to save the data inside table
	3. put  ---> to update the data 
	4. patch ---> partial update 
	5. delete --> to delete the record. 

	by default the html has get method.


2. action 
------------------------
In action we pass page reference or endpoint. Action means where to go?
	<form action="login.php">
	</form>

endpoint
----------------------
http://localhost:8080/login
http://localhost:8080/add

DRF  --> Postman / swagger tool

<form action="/login"></form>

3. enctype  --> form-data
4. id 
5. name 
6. novalidate 
7. autocomplete 

Error Code / Status code 
----------------------------------
200 
201 
400
500

----------------------------------------------------------
Student Registration
----------------------------
table 
form 
css 

fields
---------------------------
Student Name  --> text 
Gender		  --> radio button 
Age 		  --> number 
Birthdate     --> date 
Email 		  --> email
Mobile       --> number
Class 		  --> select  -->tag
Hobbies      --> checkbox
Country 	 --> select tag
State		 --> select tag
District	--> text
Taluka      --> text
Address     --> textarea tag
Upload Photo  --> file
Submit button  --> <input type='button' /><input type='submit' /><input type='reset' />


input type -->


